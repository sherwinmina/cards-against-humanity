import "./player-hand.scss";

import react from "react";
import Card from "./card";

export default function Playerhand(props) {
  const {hand, opSelectCard, selectCard, toggle, isOpen} = props;
  return <p>Playerhand</p>;
}