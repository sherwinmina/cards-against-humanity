import "./stacks.scss";

import React from "react";
import Card from "./card";

export default function Stacks(props) {
  const {stacks, opSelectedStack, selectStack, ourStackId} = props;

  return <p>Stacks</p>;
}